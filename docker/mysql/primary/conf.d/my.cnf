[mysqld]
server-id=1
log_bin=mysql-bin
binlog_format=ROW
gtid_mode=ON
enforce_gtid_consistency=ON
default_authentication_plugin=mysql_native_password

# Aqui va la configuracion real del nodo primary.
# Tu companero de BD puede completar replica user, permisos y ajustes finos.
